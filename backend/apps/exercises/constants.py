# Minimum elapsed ms before we accept a submission without logging a warning.
# Anti-cheat: catches bot submissions. Log-only in v1 — do not block.
MIN_ANSWER_MS = 200

# Maximum allowed session duration in seconds. ZEN mode passes 0 ("no limit").
MAX_SESSION_SECONDS = 3600
