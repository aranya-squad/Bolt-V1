// Figma frame 1:183 — Training Arena (mode selection)
export default function TrainingArenaPage() {
  return (
    <main style={{ minHeight: "100vh", background: "var(--color-bg-base)", padding: "var(--space-xl)" }}>
      <h1 style={{ fontFamily: "var(--font-display)", color: "var(--color-primary)" }}>
        Training Arena
      </h1>
      {/* TODO: mode cards (The Lab, Time Attack, Zen Mode, Flash Cards) — implement in Phase 4 */}
    </main>
  );
}
